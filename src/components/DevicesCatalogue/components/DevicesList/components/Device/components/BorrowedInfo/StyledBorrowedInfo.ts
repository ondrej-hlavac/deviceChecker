import { StyledVariables } from 'sharedStyledComponents/StyledVariables';
import styled from 'styled-components';

export const StyledBorrowedInfo = styled.div`
  display: flex;
  text-align: left;
  width: 100%;
  height: ${StyledVariables.BUTTON_HEIGHT};
`;
